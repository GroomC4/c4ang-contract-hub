name: Branch Build

on:
  push:
    branches:
      - develop
      - 'feature/**'

jobs:
  build:
    name: Build Avro Schemas
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Count Avro schemas
        id: count_schemas
        run: |
          AVRO_COUNT=$(find src/main/avro -name "*.avsc" | wc -l)
          echo "count=$AVRO_COUNT" >> $GITHUB_OUTPUT
          echo "📋 Found $AVRO_COUNT Avro schema files"

      - name: Generate Avro Java classes
        run: ./gradlew generateAvroJava --no-daemon

      - name: Build project
        run: ./gradlew build --no-daemon

      - name: Generate Avro documentation
        run: ./gradlew generateAvroEventDocs --no-daemon

      - name: Upload build artifacts
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts-${{ github.ref_name }}
          path: |
            build/libs/*.jar
            build/generated-main-avro-java/
            docs/generated/*.md
          retention-days: 7

      - name: Display build summary
        if: success()
        run: |
          BRANCH_NAME=$(echo "${{ github.ref_name }}" | sed 's/\//-/g')
          JAR_FILE=$(ls build/libs/*.jar | head -n 1 | xargs basename)
          JAVA_FILES=$(find build/generated-main-avro-java -name "*.java" | wc -l)

          echo "✅ Build successful!"
          echo ""
          echo "📊 Build Summary:"
          echo "  - Avro Schemas: ${{ steps.count_schemas.outputs.count }}"
          echo "  - Generated Java Classes: $JAVA_FILES"
          echo "  - JAR: $JAR_FILE"
          echo ""
          echo "📦 This branch is now available on JitPack:"
          echo ""
          echo "    implementation(\"com.github.GroomC4:c4ang-contract-hub:${BRANCH_NAME}-SNAPSHOT\")"
          echo ""
          echo "🔗 JitPack URL: https://jitpack.io/#GroomC4/c4ang-contract-hub/${BRANCH_NAME}-SNAPSHOT"
          echo ""
          echo "⏱️ Note: First request to JitPack may take 1-3 minutes to build."

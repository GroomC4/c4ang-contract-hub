spring:
  application:
    name: c4ang-contract-hub

# Kafka/Schema Registry 설정은 c4ang-platform-core에서 관리합니다.
# 이 프로젝트는 Avro 스키마 정의와 생성된 Java 클래스만 제공하는 라이브러리입니다.

logging:
  level:
    com.c4ang: DEBUG
